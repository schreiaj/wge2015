##  What Happens

We're going to type "Andrew" into the name field:

    > "A"
    (drawUserPicture is run)
    (sui recomputes)
    (drawUserPicture is run)
    > "n"
    (drawUserPicture is run)
    (sui recomputes)
    (drawUserPicture is run)
    > "d"
    (drawUserPicture is run)
    (sui recomputes)
    (drawUserPicture is run)
